import { UploadForm } from "@/components/highlights/UploadForm";

export default function DashboardHighlightsPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-start px-4 py-12 md:py-20">
      <UploadForm />
    </main>
  );
}
